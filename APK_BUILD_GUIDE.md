# 📱 دليل تحويل وبناء تطبيق أندرويد (APK) عبر GitHub Actions

تم تجهيز هذا المشروع بالكامل لإنشاء وبناء ملف **Android APK** قابل للتثبيت على أي هاتف أندرويد تلقائياً عبر **GitHub Actions CI/CD**.

---

## 🚀 خطوات البناء والتحميل في 4 خطوات بسيطة:

### 1️⃣ تصدير / رفع المشروع إلى GitHub:
- من داخل **Google AI Studio**، اضغط على قائمة الإعدادات (Settings) واختر **Export to GitHub**، أو ارفع ملفات المشروع إلى مستودع جديد على حسابك في GitHub.

### 2️⃣ الانتقال لتبويب Actions:
- افتح صفحة المستودع الخاص بك على GitHub.
- اضغط على تبويب **Actions** الموجود في الشريط العلوي للمستودع.

### 3️⃣ تشغيل البناء الآلي:
- سيعمل البناء تلقائياً بمجرد رفع الكود (`push`).
- يمكنك أيضاً تشغيله يدوياً في أي وقت:
  1. اضغط على سير العمل **Build Android APK** من القائمة اليسرى.
  2. اضغط على زر **Run workflow** ثم **Run workflow**.

### 4️⃣ تحميل وتثبيت ملف الـ APK:
- انتظر بضع دقائق حتى يكتمل البناء وتظهر علامة الصح الخضراء `✓`.
- اضغط على سجل التشغيل (Run).
- في قسم **Artifacts** بالأسفل، اضغط على **`LectureVault-Android-APK`** لتحميل ملف الـ ZIP الذي يحتوي على **`app-debug.apk`**.
- انقل الملف لهاتفك وثبته مباشرة!

---

## 🛠️ الملفات المعدّة في المشروع:
1. `.github/workflows/build-apk.yml` : سير العمل التلقائي الخاص بـ GitHub Actions (Java 17, Android SDK, Gradle, Capacitor).
2. `capacitor.config.json` : ملف إعدادات حزمة الأندرويد والهوية (`com.lecturevault.app`).
3. `package.json` : يحتوي على حزم `@capacitor/core` و `@capacitor/android` و `@capacitor/cli`.
